version: '3.8'
services:
  create-host:
    build: .
    ports:
      - "8080:8080"
    environment:
      GOTTY_AUTH: basic
      GOTTY_PASSWORD: "admin:changeme"
      GOTTY_PORT: "8080"
      GOTTY_TITLE: "VPS"
    restart: unless-stopped
